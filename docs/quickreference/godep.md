# Quick reference: godep

## Requirements

**Ideal**

- `Gopkg.lock` file present in your project

**Minimum**

- `Gopkg.toml` file present in your project

## Project discovery

Directories containing one (or both) of `Gopkg.toml` and `Gopkg.lock` are considered godep projects.
