# Quick reference: pipenv

## Requirements

**Ideal**

- `pipenv` buildtool installed
- `Pipfile.lock` file present in your project

**Minimum**

- `Pipfile.lock` file present in your project

## Project discovery

Directories containing `Pipfile.lock` files are considered pipenv projects.
