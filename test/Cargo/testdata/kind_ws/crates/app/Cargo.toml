[package]
name = "app"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = "1.0"
dual = "1.0"
myserde = { version = "1.0", package = "serde" }
shared = { workspace = true }
mystery = { workspace = true }
my_local = { path = "../libs/my_local" }
foo = { version = "1.0" }

[build-dependencies]
cc = "1.0"
dual = "1.0"

[dev-dependencies]
approx = "0.5"
dual = "1.0"

[target.'cfg(unix)'.dev-dependencies]
unixtool = "1.0"
