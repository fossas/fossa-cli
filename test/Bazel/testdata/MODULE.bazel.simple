# A simple MODULE.bazel with only bazel_dep entries
module(
    name = "my_project",
    version = "1.0.0",
)

bazel_dep(name = "rules_go", version = "0.57.0")
bazel_dep(name = "gazelle", version = "0.42.0", repo_name = "bazel_gazelle")
bazel_dep(name = "protobuf", version = "29.3")
