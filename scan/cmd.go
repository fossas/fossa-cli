package scan

import (
	logging "github.com/op/go-logging"
	"github.com/urfave/cli"
)

var log = logging.MustGetLogger("scan")

// ScanCmd detects repository conditions and creates a populated Repository and []Modules object
func ScanCmd(c *cli.Context) error {
	// TODO: implement
	return nil
}
